package com.example.cherry_pick_android.data.remote.service

import retrofit2.http.GET
import retrofit2.http.Query

interface ExamService {
    // @GET("/example")
    // suspend fun getExamData(
    //    @Query("exam") exam: Int
    // ) : Response<ResponseExamData>
}